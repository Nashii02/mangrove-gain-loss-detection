Same content as Model/metrics.example.json — kept here so the schema example lives beside the schema doc. (Optionally skip this duplicate; one canonical copy in Model/ is fine — just note it in DATA_SCHEMA.md §5.)
